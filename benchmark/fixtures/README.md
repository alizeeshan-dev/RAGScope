# Public fixtures

Only small synthetic or openly licensed fixtures belong here. Place larger representative papers for local ingestion testing in `benchmark/fixtures/representative/`; that directory is intentionally Git-ignored.
